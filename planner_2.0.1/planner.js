// daily activity time / average daily driving time
var MIN_INTENSITY_RATIO = 1.5;

var dailyPlanner = function(){
	/* data: {
		start,
		destination,
		depart_date,
		arrival_date,
		preference: {
			
		},
		intensity: 3 hours to 10 hours; // >= daily driving * MIN_INTENSITY_RATIO
		travel: {
			avarage_speed: ,
			distance_ratio: ,
		}
	}; */
	
	
	
};


//DB
var getPOINearPolyline = function(polyline, radius, limit, sortBy="totalreviews"){
	
};

